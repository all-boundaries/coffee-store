
rootProject.name = 'boundaries'

include(
        'app',
        'list',
        'utilities'
)

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            plugin('openapi-gen', 'org.openapi.generator').version('6.2.1')
        }
    }
}
