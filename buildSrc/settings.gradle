dependencyResolutionManagement {
    versionCatalogs {
        libs {
            version('spring-boot', '2.7.5')

            library('spring-boot', 'org.springframework.boot', 'spring-boot-gradle-plugin').versionRef('spring-boot')
            library('spotless', 'com.diffplug.spotless', 'spotless-plugin-gradle').version('6.11.0')
        }
    }
}